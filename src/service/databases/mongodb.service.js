import { MongoClient } from "mongodb";
import { MONGODB_ATLAS_ACCESS_STRING } from "../../config/config.js";

class mongoDBServices{
    async findId(id){
        let url = MONGODB_ATLAS_ACCESS_STRING
        let dbName = 'AssemblyDB'
    } 
}